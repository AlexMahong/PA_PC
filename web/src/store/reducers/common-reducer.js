import * as actionTypes from '../actionTypes'

const initState = {};
// 初始化公共状态
const common = (state = initState, action) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
};

export default common;